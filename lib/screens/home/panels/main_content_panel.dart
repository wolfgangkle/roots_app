import 'package:flutter/material.dart';

class MainContentPanel extends StatelessWidget {
  const MainContentPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Main Game View (Map, Village, Hero Actions...)'),
    );
  }
}
